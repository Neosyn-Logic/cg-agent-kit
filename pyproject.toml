[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "neosyn-fpga-mcp"
version = "1.1.0"
description = "MCP server for FPGA hardware design in C⏚ — drive the open-source C⏚ Verilog compiler from an AI agent."
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.10"
authors = [{ name = "Neosyn" }]
keywords = ["mcp", "model-context-protocol", "fpga", "hdl", "verilog", "eda", "rtl", "hardware", "ai", "llm"]
classifiers = [
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Topic :: Scientific/Engineering :: Electronic Design Automation (EDA)",
    "Topic :: Software Development :: Compilers",
]
dependencies = ["mcp>=1.0"]

[project.urls]
Homepage = "https://neosyn.io"
Repository = "https://github.com/Neosyn-Logic/cg-agent-kit"
"Open source" = "https://neosyn.io/open"

[project.scripts]
neosyn-fpga-mcp = "neosyn_fpga_mcp.cg_mcp_server:main"
# Kept so existing MCP host configs do not break on the rename.
cg-mcp-server = "neosyn_fpga_mcp.cg_mcp_server:main"

[tool.setuptools]
packages = ["neosyn_fpga_mcp", "cg_agent_kit"]

[tool.setuptools.package-data]
neosyn_fpga_mcp = ["*.md", "*.json", "examples/*.cg", "examples/*.md"]
